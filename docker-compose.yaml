services:
  CyberKnight:
    build:
      context: .
      dockerfile: Dockerfile
    image: chef_api
    container_name: chef_api
    ports:
      - "8081:8081"
    restart: unless-stopped

networks:
  default:
    driver: bridge
    name: chef

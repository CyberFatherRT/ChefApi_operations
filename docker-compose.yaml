services:
  CyberKnightApi:
    build:
      context: .
      dockerfile: Dockerfile
    image: cyber_knight
    container_name: cyber_knight
    ports:
      - "8081:8081"
    restart: unless-stopped

networks:
  default:
    driver: bridge
    name: cyber_knight
